# 🌸 Configuration Eterna Backend (Mode Simple)
NODE_ENV=development
PORT=8080

# Base de données (désactivée pour le moment)
# DATABASE_URL="postgresql://eterna_user:eterna_password@localhost:5432/eterna_db?schema=public"

# JWT Secret
JWT_SECRET=eterna_super_secret_jwt_key_2024
JWT_EXPIRES_IN=7d

# Redis (désactivé pour le moment)
# REDIS_URL=redis://localhost:6379

# CORS
CORS_ORIGIN=http://localhost:1420

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads
